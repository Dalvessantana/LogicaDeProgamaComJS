alert("Cálculo de area retangulo")

var base = parseInt(prompt("Digite o valor da base do seu retangulo: "))

var altura = parseInt(prompt("Digite o valor da altura do seu retangulo: "))

var area = (base * altura)

alert("A area do seu é de:" + area)